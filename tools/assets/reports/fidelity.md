# Staged reconstruction fidelity

Generated by `paperscrape-assets compare`. Each row compares a sprite
rendered from its committed SVG source against the PNG the app ships
today. **No shipped PNG is modified by this pipeline.**

Verdicts are defined in `paperscrape_assets/fidelity.py`. In short:
`PIXEL_IDENTICAL` means all four channels match everywhere;
`EDGE_EQUIVALENT` means no pixel where one image is solid and the other
empty, an exact fill colour, and every differing pixel confined to the
antialiased boundary; `DIVERGENT` means the geometry was not recovered.
`IoU` is reported but does not gate: it is an area ratio, and an
antialiased boundary is a fixed share of the perimeter, so a single
absolute threshold asks small sprites for more precision than large ones.

| Verdict | Sprites |
|---|---|
| `PIXEL_IDENTICAL` | 125 |
| `EDGE_EQUIVALENT` | 0 |
| `DIVERGENT` | 0 |

## Per sprite

| Sprite | Size | IoU | Mean alpha diff | Max alpha diff | Differing px | Solid/empty conflicts | Edge-confined | Max RGB diff | bbox delta | Verdict |
|---|---|---|---|---|---|---|---|---|---|---|
| `bar_door` | 60x84 | 1.000000 | 0.0000 | 0 | 0 / 5040 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `bar_roof_snow` | 258x30 | 1.000000 | 0.0000 | 0 | 0 / 7740 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `bar_sign` | 72x72 | 1.000000 | 0.0000 | 0 | 0 / 5184 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `bar_wall` | 270x276 | 1.000000 | 0.0000 | 0 | 0 / 74520 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `bird_body` | 90x24 | 1.000000 | 0.0000 | 0 | 0 / 2160 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `bunny_body` | 102x183 | 1.000000 | 0.0000 | 0 | 0 / 18666 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `bunny_innerear` | 45x45 | 1.000000 | 0.0000 | 0 | 0 / 2025 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `bunny_tail` | 30x30 | 1.000000 | 0.0000 | 0 | 0 / 900 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `car_body` | 291x111 | 1.000000 | 0.0000 | 0 | 0 / 32301 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `car_window` | 138x48 | 1.000000 | 0.0000 | 0 | 0 / 6624 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `cloud_body` | 798x396 | 1.000000 | 0.0000 | 0 | 0 / 316008 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `dolphin_body` | 345x174 | 1.000000 | 0.0000 | 0 | 0 / 60030 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `easteregg_pattern` | 96x57 | 1.000000 | 0.0000 | 0 | 0 / 5472 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `easteregg_shell` | 96x120 | 1.000000 | 0.0000 | 0 | 0 / 11520 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `firetruck_body` | 294x147 | 1.000000 | 0.0000 | 0 | 0 / 43218 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `firetruck_ladder` | 288x42 | 1.000000 | 0.0000 | 0 | 0 / 12096 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `firework` | 240x240 | 1.000000 | 0.0000 | 0 | 0 / 57600 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `gift_box` | 120x90 | 1.000000 | 0.0000 | 0 | 0 / 10800 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `gift_ribbon` | 120x120 | 1.000000 | 0.0000 | 0 | 0 / 14400 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `ground_flowers` | 108x36 | 1.000000 | 0.0000 | 0 | 0 / 3888 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `house_large_chimney` | 39x105 | 1.000000 | 0.0000 | 0 | 0 / 4095 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `house_large_door` | 66x135 | 1.000000 | 0.0000 | 0 | 0 / 8910 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `house_large_roof` | 450x150 | 1.000000 | 0.0000 | 0 | 0 / 67500 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `house_large_roof_snow` | 300x117 | 1.000000 | 0.0000 | 0 | 0 / 35100 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `house_large_trim` | 450x18 | 1.000000 | 0.0000 | 0 | 0 / 8100 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `house_large_wall` | 420x285 | 1.000000 | 0.0000 | 0 | 0 / 119700 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `house_shared_planter` | 78x18 | 1.000000 | 0.0000 | 0 | 0 / 1404 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `house_shared_window` | 66x63 | 1.000000 | 0.0000 | 0 | 0 / 4158 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `house_small_chimney` | 36x90 | 1.000000 | 0.0000 | 0 | 0 / 3240 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `house_small_door` | 60x114 | 1.000000 | 0.0000 | 0 | 0 / 6840 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `house_small_roof` | 318x120 | 1.000000 | 0.0000 | 0 | 0 / 38160 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `house_small_roof_snow` | 204x99 | 1.000000 | 0.0000 | 0 | 0 / 20196 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `house_small_trim` | 318x12 | 1.000000 | 0.0000 | 0 | 0 / 3816 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `house_small_wall` | 288x210 | 1.000000 | 0.0000 | 0 | 0 / 60480 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `house_window` | 66x66 | 1.000000 | 0.0000 | 0 | 0 / 4356 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `house_window_lit` | 66x66 | 1.000000 | 0.0000 | 0 | 0 / 4356 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `lightning_bolt` | 90x252 | 1.000000 | 0.0000 | 0 | 0 / 22680 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `moon_crescent` | 240x240 | 1.000000 | 0.0000 | 0 | 0 / 57600 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `moon_full` | 240x240 | 1.000000 | 0.0000 | 0 | 0 / 57600 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `moon_gibbous` | 240x240 | 1.000000 | 0.0000 | 0 | 0 / 57600 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `moon_half` | 240x240 | 1.000000 | 0.0000 | 0 | 0 / 57600 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `moon_jack_o_lantern` | 240x240 | 1.000000 | 0.0000 | 0 | 0 / 57600 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `palmtree_fronds` | 120x120 | 1.000000 | 0.0000 | 0 | 0 / 14400 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `palmtree_fronds_dead` | 120x120 | 1.000000 | 0.0000 | 0 | 0 / 14400 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `palmtree_fronds_frost` | 120x120 | 1.000000 | 0.0000 | 0 | 0 / 14400 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `palmtree_trunk` | 33x174 | 1.000000 | 0.0000 | 0 | 0 / 5742 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `penguin_beak` | 36x18 | 1.000000 | 0.0000 | 0 | 0 / 648 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `penguin_belly` | 54x102 | 1.000000 | 0.0000 | 0 | 0 / 5508 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `penguin_body` | 84x132 | 1.000000 | 0.0000 | 0 | 0 / 11088 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `penguin_feet` | 60x12 | 1.000000 | 0.0000 | 0 | 0 / 720 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_boy_summer_head_window` | 159x171 | 1.000000 | 0.0000 | 0 | 0 / 27189 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_boy_summer_walk0` | 123x255 | 1.000000 | 0.0000 | 0 | 0 / 31365 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_boy_summer_walk1` | 123x255 | 1.000000 | 0.0000 | 0 | 0 / 31365 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_boy_summer_walk2` | 123x255 | 1.000000 | 0.0000 | 0 | 0 / 31365 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_boy_winter_head_window` | 159x171 | 1.000000 | 0.0000 | 0 | 0 / 27189 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_boy_winter_walk0` | 123x255 | 1.000000 | 0.0000 | 0 | 0 / 31365 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_boy_winter_walk1` | 123x255 | 1.000000 | 0.0000 | 0 | 0 / 31365 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_boy_winter_walk2` | 123x255 | 1.000000 | 0.0000 | 0 | 0 / 31365 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_girl_summer_head_window` | 159x171 | 1.000000 | 0.0000 | 0 | 0 / 27189 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_girl_summer_walk0` | 123x255 | 1.000000 | 0.0000 | 0 | 0 / 31365 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_girl_summer_walk1` | 123x255 | 1.000000 | 0.0000 | 0 | 0 / 31365 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_girl_summer_walk2` | 123x255 | 1.000000 | 0.0000 | 0 | 0 / 31365 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_girl_winter_head_window` | 159x171 | 1.000000 | 0.0000 | 0 | 0 / 27189 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_girl_winter_walk0` | 123x255 | 1.000000 | 0.0000 | 0 | 0 / 31365 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_girl_winter_walk1` | 123x255 | 1.000000 | 0.0000 | 0 | 0 / 31365 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_girl_winter_walk2` | 123x255 | 1.000000 | 0.0000 | 0 | 0 / 31365 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_man_summer_head_car` | 120x147 | 1.000000 | 0.0000 | 0 | 0 / 17640 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_man_summer_head_window` | 159x171 | 1.000000 | 0.0000 | 0 | 0 / 27189 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_man_summer_walk0` | 123x255 | 1.000000 | 0.0000 | 0 | 0 / 31365 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_man_summer_walk1` | 123x255 | 1.000000 | 0.0000 | 0 | 0 / 31365 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_man_summer_walk2` | 123x255 | 1.000000 | 0.0000 | 0 | 0 / 31365 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_man_winter_head_car` | 120x147 | 1.000000 | 0.0000 | 0 | 0 / 17640 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_man_winter_head_window` | 159x171 | 1.000000 | 0.0000 | 0 | 0 / 27189 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_man_winter_walk0` | 123x255 | 1.000000 | 0.0000 | 0 | 0 / 31365 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_man_winter_walk1` | 123x255 | 1.000000 | 0.0000 | 0 | 0 / 31365 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_man_winter_walk2` | 123x255 | 1.000000 | 0.0000 | 0 | 0 / 31365 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_woman_summer_head_car` | 120x147 | 1.000000 | 0.0000 | 0 | 0 / 17640 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_woman_summer_head_window` | 159x171 | 1.000000 | 0.0000 | 0 | 0 / 27189 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_woman_summer_walk0` | 123x255 | 1.000000 | 0.0000 | 0 | 0 / 31365 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_woman_summer_walk1` | 123x255 | 1.000000 | 0.0000 | 0 | 0 / 31365 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_woman_summer_walk2` | 123x255 | 1.000000 | 0.0000 | 0 | 0 / 31365 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_woman_winter_head_car` | 120x147 | 1.000000 | 0.0000 | 0 | 0 / 17640 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_woman_winter_head_window` | 159x171 | 1.000000 | 0.0000 | 0 | 0 / 27189 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_woman_winter_walk0` | 123x255 | 1.000000 | 0.0000 | 0 | 0 / 31365 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_woman_winter_walk1` | 123x255 | 1.000000 | 0.0000 | 0 | 0 / 31365 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `person_woman_winter_walk2` | 123x255 | 1.000000 | 0.0000 | 0 | 0 / 31365 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `police_lightbar` | 60x18 | 1.000000 | 0.0000 | 0 | 0 / 1080 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `police_stripe` | 204x39 | 1.000000 | 0.0000 | 0 | 0 / 7956 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `pumpkin_body` | 114x90 | 1.000000 | 0.0000 | 0 | 0 / 10260 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `pumpkin_stem` | 18x36 | 1.000000 | 0.0000 | 0 | 0 / 648 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `rainbow_arc` | 594x297 | 1.000000 | 0.0000 | 0 | 0 / 176418 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `restaurant_awning` | 204x30 | 1.000000 | 0.0000 | 0 | 0 / 6120 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `restaurant_door` | 54x84 | 1.000000 | 0.0000 | 0 | 0 / 4536 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `restaurant_roof_snow` | 288x30 | 1.000000 | 0.0000 | 0 | 0 / 8640 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `restaurant_sign` | 102x105 | 1.000000 | 0.0000 | 0 | 0 / 10710 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `restaurant_wall` | 300x288 | 1.000000 | 0.0000 | 0 | 0 / 86400 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `restaurant_window` | 90x66 | 1.000000 | 0.0000 | 0 | 0 / 5940 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `road_asphalt` | 402x102 | 1.000000 | 0.0000 | 0 | 0 / 41004 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `road_curb` | 402x18 | 1.000000 | 0.0000 | 0 | 0 / 7236 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `road_line` | 54x9 | 1.000000 | 0.0000 | 0 | 0 / 486 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `sailboat_hull` | 252x51 | 1.000000 | 0.0000 | 0 | 0 / 12852 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `sailboat_sail` | 210x180 | 1.000000 | 0.0000 | 0 | 0 / 37800 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `santa_sleigh_scene` | 594x123 | 1.000000 | 0.0000 | 0 | 0 / 73062 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `santa_sleigh_trot` | 594x123 | 1.000000 | 0.0000 | 0 | 0 / 73062 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `skyscraper_canopy` | 330x24 | 1.000000 | 0.0000 | 0 | 0 / 7920 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `skyscraper_entrance` | 96x96 | 1.000000 | 0.0000 | 0 | 0 / 9216 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `skyscraper_roof_snow` | 168x27 | 1.000000 | 0.0000 | 0 | 0 / 4536 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `skyscraper_setback` | 180x96 | 1.000000 | 0.0000 | 0 | 0 / 17280 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `skyscraper_wall` | 270x450 | 1.000000 | 0.0000 | 0 | 0 / 121500 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `skyscraper_wall_lit` | 237x300 | 1.000000 | 0.0000 | 0 | 0 / 71100 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `snowman_body` | 114x222 | 1.000000 | 0.0000 | 0 | 0 / 25308 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `snowman_nose` | 39x12 | 1.000000 | 0.0000 | 0 | 0 / 468 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `snowman_scarf` | 72x18 | 1.000000 | 0.0000 | 0 | 0 / 1296 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `star_sparkle` | 180x180 | 1.000000 | 0.0000 | 0 | 0 / 32400 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `sun_body` | 240x240 | 1.000000 | 0.0000 | 0 | 0 / 57600 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `sun_glow` | 396x396 | 1.000000 | 0.0000 | 0 | 0 / 156816 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `taxi_checker` | 204x27 | 1.000000 | 0.0000 | 0 | 0 / 5508 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `tree_canopy` | 246x222 | 1.000000 | 0.0000 | 0 | 0 / 54612 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `tree_canopy_snowcap` | 228x111 | 1.000000 | 0.0000 | 0 | 0 / 25308 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `tree_dead_branches` | 246x222 | 1.000000 | 0.0000 | 0 | 0 / 54612 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `tree_fir` | 234x366 | 1.000000 | 0.0000 | 0 | 0 / 85644 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `tree_fir_snow` | 186x252 | 1.000000 | 0.0000 | 0 | 0 / 46872 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `tree_trunk` | 30x132 | 1.000000 | 0.0000 | 0 | 0 / 3960 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `water_splash0` | 162x54 | 1.000000 | 0.0000 | 0 | 0 / 8748 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |
| `water_splash1` | 162x54 | 1.000000 | 0.0000 | 0 | 0 / 8748 | 0 | yes | 0 | 0,0,0,0 | `PIXEL_IDENTICAL` |

## Sprites with no recoverable source

These ship today and cannot be regenerated. Listed so the gap is a
recorded state of the project rather than an omission from a report.

| Sprite | Why |
|---|---|
| `person_boy_summer_head_window_skin0` | Generated from person_boy_summer_head_window by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_boy_summer_head_window_skin1` | Generated from person_boy_summer_head_window by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_boy_summer_head_window_skin2` | Generated from person_boy_summer_head_window by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_boy_summer_walk0_skin0` | Generated from person_boy_summer_walk0 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_boy_summer_walk0_skin1` | Generated from person_boy_summer_walk0 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_boy_summer_walk0_skin2` | Generated from person_boy_summer_walk0 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_boy_summer_walk1_skin0` | Generated from person_boy_summer_walk1 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_boy_summer_walk1_skin1` | Generated from person_boy_summer_walk1 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_boy_summer_walk1_skin2` | Generated from person_boy_summer_walk1 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_boy_summer_walk2_skin0` | Generated from person_boy_summer_walk2 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_boy_summer_walk2_skin1` | Generated from person_boy_summer_walk2 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_boy_summer_walk2_skin2` | Generated from person_boy_summer_walk2 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_boy_winter_head_window_skin0` | Generated from person_boy_winter_head_window by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_boy_winter_head_window_skin1` | Generated from person_boy_winter_head_window by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_boy_winter_head_window_skin2` | Generated from person_boy_winter_head_window by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_boy_winter_walk0_skin0` | Generated from person_boy_winter_walk0 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_boy_winter_walk0_skin1` | Generated from person_boy_winter_walk0 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_boy_winter_walk0_skin2` | Generated from person_boy_winter_walk0 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_boy_winter_walk1_skin0` | Generated from person_boy_winter_walk1 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_boy_winter_walk1_skin1` | Generated from person_boy_winter_walk1 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_boy_winter_walk1_skin2` | Generated from person_boy_winter_walk1 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_boy_winter_walk2_skin0` | Generated from person_boy_winter_walk2 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_boy_winter_walk2_skin1` | Generated from person_boy_winter_walk2 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_boy_winter_walk2_skin2` | Generated from person_boy_winter_walk2 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_girl_summer_head_window_skin0` | Generated from person_girl_summer_head_window by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_girl_summer_head_window_skin1` | Generated from person_girl_summer_head_window by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_girl_summer_head_window_skin2` | Generated from person_girl_summer_head_window by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_girl_summer_walk0_skin0` | Generated from person_girl_summer_walk0 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_girl_summer_walk0_skin1` | Generated from person_girl_summer_walk0 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_girl_summer_walk0_skin2` | Generated from person_girl_summer_walk0 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_girl_summer_walk1_skin0` | Generated from person_girl_summer_walk1 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_girl_summer_walk1_skin1` | Generated from person_girl_summer_walk1 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_girl_summer_walk1_skin2` | Generated from person_girl_summer_walk1 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_girl_summer_walk2_skin0` | Generated from person_girl_summer_walk2 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_girl_summer_walk2_skin1` | Generated from person_girl_summer_walk2 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_girl_summer_walk2_skin2` | Generated from person_girl_summer_walk2 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_girl_winter_head_window_skin0` | Generated from person_girl_winter_head_window by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_girl_winter_head_window_skin1` | Generated from person_girl_winter_head_window by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_girl_winter_head_window_skin2` | Generated from person_girl_winter_head_window by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_girl_winter_walk0_skin0` | Generated from person_girl_winter_walk0 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_girl_winter_walk0_skin1` | Generated from person_girl_winter_walk0 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_girl_winter_walk0_skin2` | Generated from person_girl_winter_walk0 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_girl_winter_walk1_skin0` | Generated from person_girl_winter_walk1 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_girl_winter_walk1_skin1` | Generated from person_girl_winter_walk1 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_girl_winter_walk1_skin2` | Generated from person_girl_winter_walk1 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_girl_winter_walk2_skin0` | Generated from person_girl_winter_walk2 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_girl_winter_walk2_skin1` | Generated from person_girl_winter_walk2 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_girl_winter_walk2_skin2` | Generated from person_girl_winter_walk2 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_man_summer_head_window_skin0` | Generated from person_man_summer_head_window by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_man_summer_head_window_skin1` | Generated from person_man_summer_head_window by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_man_summer_head_window_skin2` | Generated from person_man_summer_head_window by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_man_summer_walk0_skin0` | Generated from person_man_summer_walk0 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_man_summer_walk0_skin1` | Generated from person_man_summer_walk0 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_man_summer_walk0_skin2` | Generated from person_man_summer_walk0 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_man_summer_walk1_skin0` | Generated from person_man_summer_walk1 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_man_summer_walk1_skin1` | Generated from person_man_summer_walk1 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_man_summer_walk1_skin2` | Generated from person_man_summer_walk1 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_man_summer_walk2_skin0` | Generated from person_man_summer_walk2 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_man_summer_walk2_skin1` | Generated from person_man_summer_walk2 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_man_summer_walk2_skin2` | Generated from person_man_summer_walk2 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_man_winter_head_window_skin0` | Generated from person_man_winter_head_window by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_man_winter_head_window_skin1` | Generated from person_man_winter_head_window by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_man_winter_head_window_skin2` | Generated from person_man_winter_head_window by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_man_winter_walk0_skin0` | Generated from person_man_winter_walk0 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_man_winter_walk0_skin1` | Generated from person_man_winter_walk0 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_man_winter_walk0_skin2` | Generated from person_man_winter_walk0 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_man_winter_walk1_skin0` | Generated from person_man_winter_walk1 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_man_winter_walk1_skin1` | Generated from person_man_winter_walk1 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_man_winter_walk1_skin2` | Generated from person_man_winter_walk1 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_man_winter_walk2_skin0` | Generated from person_man_winter_walk2 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_man_winter_walk2_skin1` | Generated from person_man_winter_walk2 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_man_winter_walk2_skin2` | Generated from person_man_winter_walk2 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_woman_summer_head_window_skin0` | Generated from person_woman_summer_head_window by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_woman_summer_head_window_skin1` | Generated from person_woman_summer_head_window by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_woman_summer_head_window_skin2` | Generated from person_woman_summer_head_window by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_woman_summer_walk0_skin0` | Generated from person_woman_summer_walk0 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_woman_summer_walk0_skin1` | Generated from person_woman_summer_walk0 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_woman_summer_walk0_skin2` | Generated from person_woman_summer_walk0 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_woman_summer_walk1_skin0` | Generated from person_woman_summer_walk1 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_woman_summer_walk1_skin1` | Generated from person_woman_summer_walk1 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_woman_summer_walk1_skin2` | Generated from person_woman_summer_walk1 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_woman_summer_walk2_skin0` | Generated from person_woman_summer_walk2 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_woman_summer_walk2_skin1` | Generated from person_woman_summer_walk2 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_woman_summer_walk2_skin2` | Generated from person_woman_summer_walk2 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_woman_winter_head_window_skin0` | Generated from person_woman_winter_head_window by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_woman_winter_head_window_skin1` | Generated from person_woman_winter_head_window by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_woman_winter_head_window_skin2` | Generated from person_woman_winter_head_window by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_woman_winter_walk0_skin0` | Generated from person_woman_winter_walk0 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_woman_winter_walk0_skin1` | Generated from person_woman_winter_walk0 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_woman_winter_walk0_skin2` | Generated from person_woman_winter_walk0 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_woman_winter_walk1_skin0` | Generated from person_woman_winter_walk1 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_woman_winter_walk1_skin1` | Generated from person_woman_winter_walk1 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_woman_winter_walk1_skin2` | Generated from person_woman_winter_walk1 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_woman_winter_walk2_skin0` | Generated from person_woman_winter_walk2 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_woman_winter_walk2_skin1` | Generated from person_woman_winter_walk2 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |
| `person_woman_winter_walk2_skin2` | Generated from person_woman_winter_walk2 by tools/generate_skin_variants.py, which moves one flat skin colour and verifies that every other colour keeps its exact pixel mask, so clothes, hair, eyes, outlines and silhouette are identical to the base by construction. `render` regenerates a sprite from an SVG and this variant has no SVG of its own: authoring one would mean hand-maintaining a third copy of every person frame per tone, which is the duplication the generator exists to remove. Re-run that script to regenerate it, not `render`. |

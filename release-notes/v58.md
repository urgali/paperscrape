🐛 Fixed
- Removed the paper-grain texture effect entirely. It was still costing real battery/performance
  even in its scaled-down form, and — more importantly — it was quietly darkening every color
  away from what you actually picked in settings. Colors should now render exactly as
  configured, and animations should be noticeably smoother.
- Beach umbrellas got a bigger, darker ground shadow so they read clearly as standing in the sand
  instead of floating.

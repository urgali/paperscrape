🐛 Fixed
- Fixed a bug where editing an already-saved/overwritten theme a second
  time and hitting "Replace with current" again would silently discard
  your new changes and re-save the old version instead. The first save
  always worked; further edits to that same theme didn't stick until now.

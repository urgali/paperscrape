🐛 Fixed
- Fixed a visible square outline that showed up behind round objects
  like the sun and moon.
- Fixed the sky's paper texture looking like a heavy, obviously
  mirrored pattern instead of a subtle grain.
- Major performance fix: animations should no longer stutter or peg
  the CPU — the paper-grain effect was being recalculated for every
  single house, tree, car, and decoration on screen, every frame. It
  now only applies to the sky, hills, and lake, where it's actually
  affordable.

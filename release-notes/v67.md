🎨 Look and feel
- Fixed the palm tree's leafy top looking slightly detached from the trunk — it now sits right
  on top, connected, as it should.
- Skyscrapers, restaurants, and bars have been redrawn to look much nicer, matching the same
  softer "paper cutout" style houses/trees/palms got last update: skyscrapers have a subtly
  rounded rooftop instead of a flat top, restaurants have a softer scalloped awning edge, and
  both restaurants and bars have the same nicer arched doors houses have.
- Same note as last time: these objects have a very subtle soft shading baked into their color
  now, so they may look a few percent darker in places than the exact color picked. Still worth
  a look on a real device.

⚙️ Under the hood
- This is "batch 2" of the larger aesthetic overhaul: skyscrapers, restaurants, and bars are now
  using the faster image-based drawing approach (only their wall/door/window/awning — window
  patterns on skyscrapers and the bar's hanging sign stay as before, since those change per
  building and don't fit a fixed image).
- Still remaining for future updates: cars, clouds, mountains, the lake, birds, sun/moon,
  seasonal decorations, and more.
- Same as last update: couldn't run a full local test build this round (no Android build tools
  in the tool environment). Please treat this as a bit less proven than usual until you've
  built/installed it yourself.

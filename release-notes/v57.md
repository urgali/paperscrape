✨ Improved
- Houses, trees, umbrellas, and buildings are now placed with smooth, continuous size and depth
  variation instead of snapping to a small number of fixed positions — matching the reference
  app's actual approach much more closely.
- Clouds now scale in count with the density setting the same way the reference app does, with
  more variety in how far away each one feels.
- Confirmed (by extracting the reference app's real rendering code) that the paper-grain texture
  belongs only on the sky, hills, and lake — everything else already looked right.
